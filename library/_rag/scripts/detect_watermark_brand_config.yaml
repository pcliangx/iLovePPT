# Brand-keyword config for detect_watermark.py (P3-22)
#
# Add vendor / template-shop brand names you want detected as watermark candidates
# in a template's text frames. Matching is case-insensitive substring on the raw
# text content; expect false positives if a legitimate slide mentions a brand.
#
# Default ships empty — users opt in.
#
# Examples (uncomment / extend as needed):
#   brands:
#     - iSlide
#     - OfficePlus
#     - 51PPT
#     - PPT之家
#     - 优品PPT
#     - Hipo
#     - Slidesgo
#     - SlidesCarnival
#     - PresentationGo
#     - PoweredTemplate

brands: []
